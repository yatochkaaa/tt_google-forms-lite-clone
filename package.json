{
  "name": "google-forms-lite-clone",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}